services:
  - type: web
    name: student-performance-backend
    env: python
    plan: free
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app

    envVars:
      - key: MONGO_URI
        value: mongodb+srv://Mukesh_D:Mukesh2006@spp.ccz5xcn.mongodb.net/?retryWrites=true&w=majority&appName=spp

      - key: PYTHON_VERSION
        value: 3.11.8

    # Optional: Keep it in the same region as your frontend for faster API calls
    region: oregon
